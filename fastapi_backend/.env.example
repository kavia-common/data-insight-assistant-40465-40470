# PostgreSQL credentials used by src/db/postgres_engine.py
# These discrete variables are composed into a SQLAlchemy URL internally (psycopg2 driver).
# SSL mode is enforced via sslmode=require in the code.

user=
password=
host=
port=
dbname=

# Used by src/db/postgres_engine.py for Postgres via SQLAlchemy (psycopg2). Include sslmode in code.
